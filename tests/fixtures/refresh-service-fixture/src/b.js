/**
 * Second fixture source for `tests/baselines/refresh-service.api.test.js`.
 * See `a.js` for why this tree exists. Deliberately a different shape from
 * `a.js` so the two rows are distinguishable in an assertion.
 */
export function greet(name) {
  return `hello, ${name}`;
}
